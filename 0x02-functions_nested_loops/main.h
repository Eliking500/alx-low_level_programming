#include <main.h>
#include <stdio.h>
#include <stdlib.h>
/**
 * main - starting point
 *
 * Return: Always 0
 */
int main(void)
{
	char h;
	h = 0;
	if (h == 0)
		printf("%d_putchar");
	else
		printf("%dnot valid");
	putchar('\n');
	return (0);
}
